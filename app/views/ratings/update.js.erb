$('#star').raty({
  readOnly: true,
  score: <%= @confesion.average_rating %>,
  path: '/assets'
});

$('#user_star').raty({
  score: <%= @confesion.score %>,
  path: '/assets',
  click: function(score, evt) {
    $.ajax({
      url: '/ratings/' + <%= @confesion.id %>,
      type: 'PATCH',
      data: { score: score }
    }).done(function (data){});
  }
});
